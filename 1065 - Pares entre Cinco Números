#include <stdio.h>
#include <string.h>
int main (void){
  int num, sum=0, i=0;
  for(i=0; i<5; i++){
    scanf("%d", &num);
    if (num%2==0){
      sum+=1;
    }
  }
  printf("%d valores pares\n", sum);
  return 0;
}
