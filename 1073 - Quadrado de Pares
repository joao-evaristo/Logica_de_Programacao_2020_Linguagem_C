#include <stdio.h>
#include <math.h>
int main()
{
  int num, i=0,quad;
  scanf("%d", &num);
  for (i=1; i<=num; i++){
    if (i%2 ==0){
      quad = pow(i, 2);
      printf("%d^2 = %d\n", i, quad);
    }
  } 
  return 0;
}
